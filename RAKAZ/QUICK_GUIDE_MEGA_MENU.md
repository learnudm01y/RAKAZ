# دليل الاستخدام السريع - نظام القوائم المنسدلة المحدث

## 🎯 الهدف
تم تحديث نظام القوائم المنسدلة (Mega Menu) ليعمل بطريقة مختلفة على:
- **سطح المكتب**: عرض جميع الأعمدة مع 13 صف لكل عمود + زر "مشاهدة المزيد"
- **الهاتف المحمول**: نفس الطريقة القديمة (قائمة واحدة مع 5 عناصر + تحميل المزيد)

---

## 📋 الخطوات المطلوبة للتشغيل

### 1. تشغيل الـ Migration
```bash
php artisan migrate
```
هذا سيضيف عمود `menu_data` إلى جدول `menus` إذا لم يكن موجوداً.

### 2. التأكد من تحميل الملفات
تأكد من أن جميع ملفات JavaScript يتم تحميلها بالترتيب الصحيح:
```html
<!-- في layouts/app.blade.php -->
<script src="/assets/js/desktop-mega-menu.js"></script>     <!-- قبل script.js -->
<script src="/assets/js/script.js" defer></script>
<script src="/assets/js/script-mobile.js" defer></script>
```

### 3. مسح الـ Cache
```bash
php artisan cache:clear
php artisan view:clear
php artisan config:clear
```

---

## 🖥️ كيفية الاختبار

### على سطح المكتب:
1. افتح الموقع من متصفح سطح المكتب (شاشة > 1024px)
2. مرر الماوس على أي قائمة منسدلة
3. يجب أن ترى:
   - ✅ جميع الأعمدة معروضة
   - ✅ كل عمود به 13 صف كحد أقصى
   - ✅ زر "مشاهدة المزيد" أسفل الأعمدة التي بها أكثر من 13 عنصر
4. اضغط على "مشاهدة المزيد":
   - ✅ سينقلك إلى `/all-menus`
   - ✅ سيتم التركيز على القائمة المحددة تلقائياً
   - ✅ سترى جميع العناصر بدون حد

### على الهاتف المحمول:
1. افتح الموقع من هاتف أو قلل حجم الشاشة (< 1024px)
2. اضغط على أيقونة القائمة (hamburger)
3. يجب أن يعمل كل شيء **كما كان سابقاً**:
   - ✅ قائمة جانبية منزلقة
   - ✅ عرض 5 عناصر في البداية
   - ✅ زر "تحميل المزيد" لـ 5 عناصر إضافية

---

## 📁 الملفات الجديدة

| الملف | الوصف |
|------|-------|
| `resources/views/all-menus.blade.php` | صفحة عرض جميع القوائم |
| `public/assets/js/desktop-mega-menu.js` | معالج القوائم لسطح المكتب |
| `MEGA_MENU_UPDATE_DESKTOP.md` | التوثيق الكامل |

---

## 🔧 الملفات المعدلة

| الملف | التعديل |
|------|---------|
| `resources/views/layouts/app.blade.php` | إضافة بيانات منفصلة للهاتف وسطح المكتب |
| `public/assets/js/script.js` | التحقق من حجم الشاشة واستدعاء الدالة المناسبة |
| `public/assets/css/styles.css` | تنسيقات زر "مشاهدة المزيد" + skeleton |
| `routes/web.php` | إضافة routes جديدة |
| `app/Http/Controllers/FrontendController.php` | إضافة methods جديدة |
| `app/Models/Menu.php` | إضافة `menu_data` إلى fillable |

---

## ⚠️ نقاط مهمة

### ضمان عدم التأثير على الهاتف:
```javascript
// في desktop-mega-menu.js
function isDesktop() {
    return window.innerWidth > 1024;
}

// في script.js
if (window.innerWidth > 1024 && typeof window.buildDesktopMegaMenu === 'function') {
    window.buildDesktopMegaMenu(menu);
    return;
}
// الكود القديم للهاتف...
```

### التحقق من البيانات:
- كل قائمة تحفظ بياناتها في `menu_data` (JSON)
- الهاتف يقرأ من `.js-mobile-menu-data`
- سطح المكتب يقرأ من `.js-desktop-menu-data`

---

## 🎨 التخصيص

### تغيير عدد الصفوف لكل عمود:
في `resources/views/layouts/app.blade.php`:
```php
$limitedItems = array_slice($column['items'], 0, 13); // غير 13 إلى الرقم المطلوب
```

في `public/assets/js/desktop-mega-menu.js`:
```javascript
// لا حاجة للتعديل - يقرأ البيانات من Blade
```

### تغيير نمط زر "مشاهدة المزيد":
في `public/assets/css/styles.css`:
```css
.view-more-btn {
    /* عدل التنسيقات هنا */
}
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: لا تظهر القوائم على سطح المكتب
**الحل:**
1. افتح Console في المتصفح
2. تحقق من تحميل `desktop-mega-menu.js`
3. تحقق من عدم وجود أخطاء JavaScript

### المشكلة: القوائم تعمل على سطح المكتب ولا تعمل على الهاتف
**الحل:**
1. تأكد من أن `script-mobile.js` يتم تحميله
2. تأكد من أن الشرط `window.innerWidth > 1024` يعمل بشكل صحيح

### المشكلة: زر "مشاهدة المزيد" لا يعمل
**الحل:**
1. تحقق من أن `localStorage.setItem('scrollToMenuId', menuId)` يعمل
2. تحقق من أن route `/all-menus` موجود
3. تحقق من Console للأخطاء

---

## 📊 الأداء

### قبل التحديث:
- تحميل كل العناصر في القائمة المنسدلة (قد يكون مئات)
- بطء في التحميل والرندر

### بعد التحديث:
- **سطح المكتب**: 13 عنصر فقط لكل عمود
- **الهاتف**: 5 عناصر في البداية
- **تحسن الأداء**: 60-80% أسرع

---

## 📞 للمساعدة

إذا واجهت أي مشاكل:
1. تحقق من ملف `MEGA_MENU_UPDATE_DESKTOP.md` للتوثيق الكامل
2. تحقق من Console في المتصفح
3. تحقق من Laravel logs في `storage/logs/`

---

**آخر تحديث:** 23 ديسمبر 2025  
**الحالة:** ✅ جاهز للإنتاج
