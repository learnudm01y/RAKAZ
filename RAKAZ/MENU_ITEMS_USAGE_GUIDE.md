# دليل استخدام نظام إضافة العناصر للقوائم

## 🎯 نظرة عامة

تم إصلاح وتحسين نظام إضافة العناصر لأعمدة القوائم بحيث يعمل بطريقتين:

### الطريقة الأولى: الربط بتصنيف موجود
1. اختر تصنيفاً من قائمة "ربط بتصنيف"
2. سيتم **تعطيل** الحقول المخصصة تلقائياً (الاسم المخصص والرابط المخصص)
3. سيستخدم النظام اسم التصنيف ورابطه تلقائياً

### الطريقة الثانية: إنشاء رابط مخصص
1. اترك قائمة "ربط بتصنيف" **فارغة**
2. ستكون الحقول المخصصة **نشطة** ويمكنك الكتابة فيها
3. املأ:
   - **الاسم المخصص بالعربي** (اختياري)
   - **الاسم المخصص بالإنجليزي** (اختياري)
   - **الرابط المخصص** (اختياري)
4. يجب ملء **على الأقل** حقل واحد من الحقول المخصصة

## ✅ التحسينات التي تم إضافتها

### 1. إزالة `disabled` الافتراضي
- الحقول المخصصة الآن **نشطة** افتراضياً
- تُعطّل فقط عند اختيار تصنيف

### 2. تحسين JavaScript
```javascript
function toggleCustomFields(select, columnId) {
    const customFields = document.querySelectorAll('.custom-field-' + columnId);
    const hasCategory = select.value !== '';
    
    customFields.forEach(field => {
        if (hasCategory) {
            // عطّل الحقول وامسح محتواها
            field.disabled = true;
            field.value = '';
            field.style.backgroundColor = '#f3f4f6';
            field.style.cursor = 'not-allowed';
        } else {
            // فعّل الحقول
            field.disabled = false;
            field.style.backgroundColor = 'white';
            field.style.cursor = 'text';
        }
    });
}
```

### 3. Validation في Controller
```php
// تحقق من وجود إما category أو بيانات مخصصة
if (!$request->category_id && !$request->custom_name_ar && !$request->custom_name_en && !$request->custom_link) {
    return back()->withErrors([
        'error' => 'يجب اختيار تصنيف أو ملء البيانات المخصصة'
    ])->withInput();
}
```

### 4. Validation في JavaScript (Frontend)
- رسالة واضحة ومفصلة عند محاولة الإرسال بدون بيانات
- تسليط الضوء على الحقول المطلوبة (Border برتقالي)
- إزالة التسليط تلقائياً بعد 3 ثوانٍ

### 5. نص توضيحي بارز
صندوق أزرق مع أيقونة 📌 يشرح الآلية:
```
📌 ملاحظة: اختر تصنيفاً من القائمة لربطه تلقائياً، أو اترك القائمة فارغة 
واملأ الحقول المخصصة لإنشاء رابط مخصص.
```

### 6. تحسين CSS
```css
.form-control:disabled {
    background-color: #f3f4f6 !important;
    color: #9ca3af;
    cursor: not-allowed;
    opacity: 0.6;
}
```

## 📋 أمثلة الاستخدام

### مثال 1: ربط بتصنيف "Traditional Kandoras"
1. اختر "Traditional Kandoras" من القائمة
2. الحقول المخصصة ستُعطّل تلقائياً
3. اضغط إضافة ✅
4. سيظهر العنصر باسم التصنيف ورابطه

### مثال 2: إنشاء رابط مخصص "عروض خاصة"
1. اترك قائمة التصنيفات فارغة
2. املأ:
   - الاسم بالعربي: "عروض خاصة"
   - الاسم بالإنجليزي: "Special Offers"
   - الرابط: "https://example.com/special-offers"
3. اضغط إضافة ✅
4. سيظهر العنصر بالاسم والرابط المخصص

### مثال 3: رابط خارجي
1. اترك قائمة التصنيفات فارغة
2. املأ:
   - الاسم بالعربي: "تابعنا على إنستغرام"
   - الاسم بالإنجليزي: "Follow us on Instagram"
   - الرابط: "https://instagram.com/yourpage"
3. اضغط إضافة ✅

## ⚠️ رسائل الخطأ المحتملة

### رسالة: "يجب اختيار تصنيف أو ملء البيانات المخصصة"
**السبب:** لم تختر تصنيفاً ولم تملأ أي حقل مخصص

**الحل:** اختر واحداً من:
- اختر تصنيفاً من القائمة
- أو املأ الحقول المخصصة

### رسالة: "The custom link field must be a valid URL"
**السبب:** الرابط المخصص غير صحيح

**الحل:** تأكد أن الرابط يبدأ بـ `http://` أو `https://`
مثال: `https://example.com`

## 🎨 التغييرات البصرية

### الحقول النشطة (Enabled)
- خلفية بيضاء
- حدود رمادية
- مؤشر نص عادي

### الحقول المعطلة (Disabled)
- خلفية رمادية فاتحة (#f3f4f6)
- نص رمادي خفيف
- مؤشر "not-allowed"
- شفافية 60%

### عند التحقق من الأخطاء
- حدود برتقالية (#f59e0b)
- تستمر 3 ثوانٍ ثم تختفي

## 🔧 الملفات المُعدّلة

1. ✅ `resources/views/admin/menus/columns.blade.php`
   - إزالة `disabled` من الحقول
   - تحسين JavaScript
   - إضافة validation
   - إضافة نص توضيحي

2. ✅ `app/Http/Controllers/Admin/MenuController.php`
   - تحسين validation rules
   - إضافة تحقق مخصص
   - رسائل خطأ واضحة

3. ✅ `public/assets/css/admin-menu.css`
   - تحسين تصميم الحقول المعطلة
   - إضافة transitions
   - تحسين الألوان

## ✨ النتيجة النهائية

الآن النظام:
- ✅ يعمل بشكل صحيح 100%
- ✅ واضح ومفهوم للمستخدم
- ✅ يمنع الأخطاء بـ validation قوي
- ✅ يوفر تجربة مستخدم ممتازة
- ✅ يدعم العربية والإنجليزية بالكامل

---

**تم التحديث:** 10 ديسمبر 2025
**الحالة:** ✅ جاهز ومُختبر
